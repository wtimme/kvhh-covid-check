doctype html

html(lang="de")
  include partials/head.pug
  
  body
    include partials/navigation.pug

    div.container
      div.jumbotron.mt-5
        h1 Jetzt Corona-Testergebnis abfragen

        form
          
          div.form-group
            label(for="code") Code
            input.form-control(type="text" id="code" name="code" aria-describedby="codeHelp" placeholder="U80001534" required)
            small.form-text.text-muted(id="codeHelp") Gib hier den Code ein, den Du vom Arztruf erhalten hast.
          
          button.btn.btn-primary.mb-2(type="submit") Prüfen
        
        p.lead Dieser Service wird #[i nicht] von der Kassenärztlichen Vereinigung Hamburg bereitgestellt.

        p.text-muted Alle Angaben sind ohne Gewähr. Keine Garantie auf Vollständigkeit.
    
      include partials/footer.pug

    script(src='/js/jquery.min.js')
    script(src='/js/popper.min.js')
    script(src='/js/bootstrap.min.js')
